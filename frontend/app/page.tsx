import Layout from "./(main)/layout";
import { Title } from "@mantine/core";

export default function MainPage() {
    return (
        <Layout>
            <Title>Main</Title>
        </Layout>
    );
}
